services:
  - type: web
    name: smarttaxagent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn smarttaxagent-api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENAI_API_KEY
        sync: false
